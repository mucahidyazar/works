// import { mockNetWorkResponse } from '@utils';

// import { store } from '../index';
// import { getArticles } from './slice';

describe('Get all articles', () => {
  // beforeAll(() => {
  //   mockNetWorkResponse({});
  // });

  // it('Should be able to create a new article', async () => {
  //   const result = await store.dispatch(getArticles({}));
  //   const article = result.payload;

  //   expect(result.type).toBe('articles/get/fulfilled');
  //   expect(article.docs.length).toEqual(10);
  // });
  it('it should pass', () => {
    expect(true).toBe(true);
  });
});
